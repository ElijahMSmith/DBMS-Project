export default class User {
    constructor(username, email, uid, unid, permission) {
        this.username = username;
        this.email = email;
        this.uid = uid;
        this.unid = unid;
        this.permission = permission;
    }
}
