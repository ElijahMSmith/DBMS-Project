export default class User {
    constructor(username, email, uid, unid, permLevel) {
        this.username = username;
        this.email = email;
        this.uid = uid;
        this.unid = unid;
        this.permLevel = permLevel;
    }
}
